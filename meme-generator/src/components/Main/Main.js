import MemeForm from "../MemeForm/MemeForm";
import MemePreview from "../MemePreview/MemePreview";

function Main() {
  return (
    <main className="main">
      <MemePreview />
      <MemeForm />
    </main>
  );
}

export default Main;
